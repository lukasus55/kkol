'use client';

export default function AccountTab({ user }: { user: any }) {
  return (
    <div className="tab_content tab_content-account active">
      <h2>Konto (W budowie)</h2>
    </div>
  );
}
