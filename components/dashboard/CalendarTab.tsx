'use client';

export default function CalendarTab({ user }: { user: any }) {
  return (
    <div className="tab_content tab_content-calendar active">
      <h2>Kalendarz (W budowie)</h2>
    </div>
  );
}
