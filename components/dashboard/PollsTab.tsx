'use client';

export default function PollsTab({ user }: { user: any }) {
  return (
    <div className="tab_content tab_content-polls active">
      <h2>Głosowania (W budowie)</h2>
    </div>
  );
}
