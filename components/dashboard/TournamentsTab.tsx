'use client';

export default function TournamentsTab({ user }: { user: any }) {
  return (
    <div className="tab_content tab_content-tournaments active">
      <h2>Turnieje (W budowie)</h2>
    </div>
  );
}
